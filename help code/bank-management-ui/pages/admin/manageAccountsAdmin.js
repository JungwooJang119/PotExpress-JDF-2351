import Head from "next/head";
import Image from "next/image";
import styles from "../../styles/Home.module.css";
import { url } from "../../lib/env";
import { TextField, Button } from "@mui/material";
import { useState } from "react";
import Link from "next/link";

export default function CustomerMenu() {
  return (
    <div className={styles.container}>
      <Head>
        <title>Bank Management UI Home</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main className={styles.main}>
        <h2 className={styles.title}>Manage Accounts</h2>

        <div className={styles.grid}>
          <Link href={"/admin/addOwner"}>
            <a className={styles.card}>
              <h2>Add Owner &rarr;</h2>
            </a>
          </Link>

          <Link href={"/admin/removeOwner"}>
            <a className={styles.card}>
              <h2>Remove Owner &rarr;</h2>
            </a>
          </Link>

          <Link href={"/admin/createAccount"}>
            <a className={styles.card}>
              <h2>Create New Account &rarr;</h2>
            </a>
          </Link>

          <Link href={"/admin/adminMenu"}>
            <Button fullWidth color="error" variant="contained">
              Back
            </Button>
          </Link>
        </div>
      </main>
    </div>
  );
}
